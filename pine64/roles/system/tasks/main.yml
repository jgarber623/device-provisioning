- name: Install packages
  import_role:
    name: common

- name: Copy configuration files
  copy:
    dest: "{{ item.dest }}"
    src: "{{ item.src }}"
  with_items: "{{ files_to_copy }}"
  become: yes
  notify:
    - restart ssh
